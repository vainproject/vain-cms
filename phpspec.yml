suites:

    app:
        namespace: Vain
        psr4_prefix: Vain
        spec_path: spec/app
        src_path: app

    modules:
        namespace: Modules
        psr4_prefix: Modules
        spec_path: spec/modules
        src_path: modules

